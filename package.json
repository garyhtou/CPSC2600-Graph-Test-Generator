{
	"name": "cpsc2600-graph-tester",
	"version": "1.0.0",
	"description": "Test CPSC2600 Graph assignment (HW5)",
	"main": "dist/index.js",
	"repository": "https://github.com/garyhtou/cpsc2600-graph-tester",
	"author": "Gary Tou <gary@garytou.com> (https://garytou.com)",
	"prettier": "@garyhtou/prettier-config",
	"scripts": {
		"start": "npm run build && ts-node .",
		"build": "rimraf ./dist && tsc",
		"dev": "ts-node-dev --respawn --pretty --transpile-only src/index.ts",
		"lint": "tslint -c tslint.json -p tsconfig.json --fix",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"@garyhtou/prettier-config": "^1.0.1",
		"@types/node": "^16.11.9",
		"ts-node-dev": "^1.1.8",
		"tslint": "^6.1.3"
	},
	"dependencies": {
		"dotenv": "^10.0.0",
		"firebase-admin": "^10.0.0",
		"js-graph-algorithms": "^1.0.18",
		"rimraf": "^3.0.2",
		"ts-node": "^10.4.0",
		"typescript": "^4.5.2"
	}
}
